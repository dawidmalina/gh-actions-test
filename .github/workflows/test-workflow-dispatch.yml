name: Terraform Apply

on:
  workflow_dispatch:
    inputs:
      project_path:
        description: "Define project path"
        required: true
        type: string

jobs:
  call-terraform-workflow:
    name: "workflow"
    uses: dawidmalina/actions-hub/.github/workflows/terraform-apply-gcp.yml@main
    with:
      product: 'astrato'
    secrets: inherit
